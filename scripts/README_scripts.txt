
These scripts were designed to run from a directory
one level above the root of this repository.

The input data and ancillary are also subdirectories
of that directory.

And intermediate and final directories are also created
in that directory.
